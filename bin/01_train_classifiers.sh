#!/usr/bin/env bash

# for each dataset train (SUBSTITUTE CLASSIFIERS)
## 1) lstm classifier
## 2) freezed bert + dense layers classifier
# copy all `model.tar.gz` to ./presets folder
# create a table with metrics on the valid/test splits (save to ./results)

